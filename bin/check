#!/bin/sh

set -eu

if [ "$#" -gt 1 ]; then
  echo "Usage: bin/check [--fix]" >&2
  exit 2
fi

case "${1:-}" in
  '')
    npm run lint
    ;;
  --fix)
    npm run lint:fix
    ;;
  *)
    echo "Usage: bin/check [--fix]" >&2
    exit 2
    ;;
esac

npm run precheck
